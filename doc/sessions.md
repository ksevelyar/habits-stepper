# Sessions
* [x] use ring buffer of nvs to store last 1024 sessions permanently
* [x] ended_at updates after one minute of inactivity
* [ ] sends single session record to backend on session end via json POST
